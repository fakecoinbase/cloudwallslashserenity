apiVersion: apps/v1
kind: Deployment
metadata:
  name: phemex-recorder
  labels:
    app: phemex-recorder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: phemex-recorder
  template:
    metadata:
      labels:
        app: phemex-recorder
    spec:
      containers:
        - name: phemex-recorder
          image: cloudwallcapital/serenity:2020.04.13-b6
          command: ["python"]
          args: ["serenity/mdrecorder/phemex_fh.py"]
          volumeMounts:
            - mountPath: /behemoth
              name: behemoth 
      volumes:
        - name: behemoth
          persistentVolumeClaim:
            claimName: behemoth-pv-claim
